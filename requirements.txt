# Industrial AI MVP -- pinned dependencies
# Python 3.11+ recommended (developed/tested on 3.13).
#
# Versions are pinned with compatible-release ranges so a fresh install is
# reproducible while still receiving patch fixes. For a fully frozen build,
# run `pip freeze > requirements.lock.txt` after install.

numpy>=1.26,<3
pandas>=2.1,<4
PyYAML>=6.0,<7
scikit-learn>=1.6,<1.8
xgboost>=2.1,<3.1
optuna>=4.1,<5
shap>=0.46,<0.49
fastapi>=0.115,<0.121
uvicorn>=0.34,<0.40
pydantic>=2.7,<3

# --- dev / test ---
pytest>=8.3,<9
httpx>=0.27,<0.29
